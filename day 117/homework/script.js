class mathHelpers {
    constructor(PI){
        this.PI = PI
    }

    power(firstNum, secondNum) {
        this.first = firstNum
        this.second = secondNum
        return Math.pow(firstNum, secondNum)
    }
}